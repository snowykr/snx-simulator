
- Added a separate `.github/workflows/test.yml` workflow instead of modifying `publish.yml` so test verification stays isolated from release publishing concerns, matching the plan's requirement to keep publish CI untouched.
- Kept the new smoke test intentionally narrow: one instruction-only compile/run case and one invalid-opcode failure case. This establishes a stable baseline without changing runtime logic or pre-empting DW-specific behavior.
- Added DW-ready model types in `snx/ast.py` (`DirectiveNode`, `TypedSymbol`, `DataImageWord`) and made them additive to the existing instruction pipeline by keeping `Line.directive` optional and `IRProgram.instructions` unchanged.
- Mirrored `typed_symbols` and `initial_data_image` on `CompileResult` with concrete empty defaults and `ir`-based normalization, so instruction-only callers keep working while later DW tasks can populate richer metadata.
- Re-exported the new DW metadata types from `snx.__init__` so Python API consumers can construct and inspect the additive model layer without reaching through undocumented internals.
- Recognized `DW` in `snx/parser.py` by checking the normalized identifier before opcode parsing, which keeps `DW` case-insensitive while preserving all existing non-`DW` instruction and operand parsing paths.
- Emitted exactly one parser diagnostic `P007` (`DW requires one or more signed decimal initializers`) for empty or invalid initializer lists and returned no directive node for those lines, so invalid `DW` input cannot fall through into `S001` unknown-instruction handling.
- Added a small additive `Line.content` helper and a `Token.matches_ident(...)` helper to keep directive-aware parser/model code explicit without widening tokenizer grammar or changing instruction-only behavior.
- Kept duplicate-name handling global by defining all labels through a single analyzer symbol table and only mirroring `CODE` symbols into the legacy `ir.labels` map, so Task 4 adds typed domains without relaxing existing `S006` behavior.
- Chose to emit `I002` as a warning while normalizing each DW initializer through the existing 16-bit word helper, and to emit `M002` per out-of-bounds allocated word while still advancing the logical data counter for deterministic source-order allocation.
- Implemented operand-context label resolution in `snx/analyzer.py` rather than the parser or encoder so the existing parser can keep producing bare `LabelRefOperand`s, while the analyzer lowers only the valid `LD`/`ST`/`LDA` data-label cases into `$0`-based `AddressOperand`s.
- Kept `BZ` and label-form `BAL` on the existing code-label path by validating them against `typed_symbols` for domain correctness but leaving successful operands as `LabelRefOperand`s, which preserves `IRProgram.labels` as the branch-encoding map.
- Chose stable diagnostic messages `Code label '<name>' cannot be used as a data address` (`S007`) and `Data label '<name>' cannot be used as a code target` (`S008`) so cross-domain misuse stays explicit and testable.
- Kept Task 6 runtime-only by consuming `ir_program.initial_data_image` during simulator construction instead of threading new constructor parameters or treating instructions as DMEM contents; this makes `from_compile_result(...)` and `from_source(...)` inherit preload behavior automatically while preserving existing execution semantics.
- Kept Task 7 scoped to `tests/test_dw_api.py` because the existing `snx.encoding.encode_program` and `snx.parser.parse_code` implementations already satisfy the required compatibility contract; adding regression tests proves DW metadata is additive without risking behavior changes in public helpers.
